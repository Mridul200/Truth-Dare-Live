## Packages
framer-motion | Page transitions and fluid UI animations
canvas-confetti | Delightful interactions for game events
@types/canvas-confetti | Types for canvas-confetti

## Notes
WebSocket connects to /ws path.
UI assumes server sends roomUpdate events with full RoomState payload.
Client state manages playerName to map to the correct player ID in the room.
